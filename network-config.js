// Optional deployment override. Omit to use the public PeerJS signaling service.
// For networks requiring TURN, supply short-lived credentials from your own service.
// Never commit long-lived TURN secrets to a public repository.
window.SPIN_NETWORK_CONFIG={};
